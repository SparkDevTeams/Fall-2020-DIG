import React from 'react';

const TipsContainer = (props) => {
  return (
    <React.Fragment>
      <h1>hello this is the tips container {props.index}</h1>
    </React.Fragment>
  );
};

export default TipsContainer;
